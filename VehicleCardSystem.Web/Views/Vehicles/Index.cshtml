@model IEnumerable<VehicleCardSystem.Core.Models.Vehicle>

@{
    ViewData["Title"] = "Vehicles";
}

<main aria-labelledby="title">
    <div class="d-flex justify-content-between align-items-center mb-3">
        
        <h3 id="title" class="mb-0">@ViewData["Title"]</h3> 
        <a asp-action="Create" class="btn btn-primary"><i class="fas fa-plus-circle"></i> Create New</a>
    </div>
    <table class="table table-striped table-bordered">
        <thead class="thead-dark">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Plate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.VehicleTypes)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ModelYear)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Color)
                </th>
               
                <th></th>
            </tr>
        
        </thead>
        <tbody>
    @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Plate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.VehicleTypes.ModelName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ModelYear)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Color)
                </td>
                
                <td>
                        <a asp-action="Edit" asp-route-id="@item.VehicleId" class="btn btn-sm btn-warning"><i class="fas fa-edit"></i> Update</a> |
                        <a asp-action="Details" asp-route-id="@item.VehicleId" class="btn btn-sm btn-info"><i class="fas fa-circle-info"></i> Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.VehicleId" class="btn btn-sm btn-danger"><i class="fas fa-xmark"></i> Delete</a>
                </td>
            </tr>
    }
        </tbody>
    </table>
</main>